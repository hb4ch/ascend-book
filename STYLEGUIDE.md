# 《昇腾平台技术实战》写作规范（风格指南）

> 本文档是 **M0 评审产出**，全书写作必须遵守。目标：**不写水货、以源码为准、术语统一、可溯源、可运行性分级清晰**。

## 1. 定位与读者

- 读者：有 C++/深度学习基础、**无昇腾经验**的实操型工程师。
- 书不是官方文档的转述，而是对 CANN 开源仓源码的**拆解 + 拼接 + 原理推导**。
- 每条技术论断必须至少来自：源码、仓内文档、官方文档，三选一并标注。

## 2. 来源标注规范（强制：脚注化）

**正文禁止出现 `📦 源码:` / `📄 资料:` 行内标注**（会打断阅读）。规则：

- 正文关键论断用 Markdown **脚注** `[^n]`，脚注本体在**章末「本章来源与进一步阅读」**统一聚合：`路径 | 一句话用途 | 状态说明`。
- 脚注里一律以仓库相对根写路径（如 `asc-devkit/impl/...`），不带机器绝对前缀；许可证按素材类型注明（CANN Open 2.0 / 官方文档）。
- 全局账本：`docs/附录/appD-source-map.md` 每章→源仓路径总表，持续维护；它才是「海量引用」的家。
- 例外：一句话引用（非论断）可不用脚注，但首次出现的仓库名仍应在章末清单里。

## 3. 代码与运行性分级

**代码四句法**（本编强制，全书推荐）：每段代码遵循「①一句话人话总述 → ②关键行真码（≤25 行）→ ③机制解释 → ④可运行性提示」。可运行性整段只标一次，四种分级：

| 标记 | 含义 |
|---|---|
| `[可在 NPU 运行]` | 仓内已验证的可运行样例，读者在真实环境可直接跑 |
| `[可用 CPU-SIM 运行]` | 在本机 CPU Simulator（如 pto-isa CPU-Sim）可运行 |
| `[需真机验证]` | 逻辑/API 正确但本编无法真机验证，给出验证步骤 |
| `[示意代码]` | 简化的原理示意，不保证可编译运行 |

分级标签放代码块首行注释或紧跟代码块的一行，不逐行标注。

## 4. 章节结构规范

每章统一骨架：

```
# 第N章 标题
> 章首引用/动机一句
## 本章目标与阅读指引     （含「本节要点」）
...正文若干二级节（每节可含「陷阱与注意」框）
## 陷阱与注意（汇总）
## 进一步阅读
```

- 核心章目标字数 **8k–12k 中文字**；不达标不发布（word:count 校验）。
- 表述使用中文书面语；技术名词首次出现时给出中英对照：「运行时（runtime）」。
- 禁用：「昇腾很强大」「性能提升数倍」之类无出处论断；需要时给出可复现的 benchmark 出处。

## 5. 术语一致性

- 唯一定义源：`glossary.md`（同步进站点附录B）。
- 新增术语流程：写进 `glossary.md` → 全书检索替换 → 通过 `npm run check:terms`。
- 高频术语示例：`runtime`（运行时）、`stream`（流）、`task`（任务）、`GE`（图引擎）、`ACLN/N`（AscendCL 算子）、`tile`（分块）、`IR`（中间表示）、`MPMD`、`单边通信`。中英择一，全书不改。

## 6. 图表规范（本编以图表激发）

- 复用官方图：复制进 `docs/**/figures/` 或 `figures/`，并标注源文件路径 + 许可证（CANN Open 2.0）；文件名带 `src-` 前缀。
- 自绘图：**优先用 Mermaid**（flowchart/sequence/state 三件套），站点已启用 `vitepress-plugin-mermaid`；**分层/架构/数据流类大图可用手写 SVG**（存 `docs/figures/`，统一配色：用户态绿 #2f9e63、内核态蓝 #4263eb、硬件橙 #e8590c、同步/控制紫 #9c36b5，中文字体栈 PingFang/微软雅黑），两者都需图注与正文解释；风格统一（同色系、同字号、每图必有一句话图注）。
- **一章一图**：每章至少一张能背下来的小结图（本书回忆锚点）。
- 图片/图必须有中英文 alt；正文必须对图做至少一句解释（不解释的图不放）。
- **表格渲染纪律**：单元格内不能出现未转义的 `|`（代码/路径里的 `|` 用 `\|`）；表头/分隔行语法必须完整；表格后留空行。每轮 review 检查表格渲染。

## 7. 自我一致性检查（每章成稿后）

1. `npm run docs:build` 通过；
2. `npm run check:links` 通过；
3. `npm run check:source` 通过：引用的源码路径在 `/mnt/SATASSDEXT4/cann/<repo>/...` 真实存在；
4. `npm run check:terms` 通过；
5. 无遗留 TODO/占位；每章与本书四大能力目标对齐（认知/实操/底层/现代后端）。

## 8. 争议与边界（明示性写作）

- **内核驱动源码不在开源仓**：第6章只写「用户态可观测的能力 + 内核边界概念」，明确标注资料性质。
- **BiSheng 编译器源码不在开源仓**：第12章基于文档/产物/RTC/simulator 写作工具链使用级详解。
- **无 NPU 硬件**：本机不能真机验证；PTO 部分以 CPU-Sim 验证；Ascend C 样例标注 `[需真机验证]` 并给出步骤。

## 9. 迭代与评审（第一编三轮制）

每轮 = plan → write → review：

1. **proof read**：每章写完后全篇重读一遍——错别字、句式、表意；引用路径与 API 拼写逐字核；
2. **表格渲染检查**：`npm run docs:build` 后抽查生成的 HTML，确认表格/脚注/Mermaid 正常；发现含裸 `|` 的单元格立即修正；
3. **图表充足性**：每章至少 1 张 Mermaid 小结图 + 若干示意小图，图注齐全；
4. 结束后跑 `npm run verify`，并在 `CHANGELOG.md` 记录本轮结论。

## 10. 第二编补充约束（M2，在 §1~§9 之上追加）

第二编（第4–8章，runtime/驱动/DFX/内存）源码最厚，最易写成源码翻译流水账。三条纪律：

1. **证据密度服务叙事，不追源码行数**。每章只选 ≤5 个「锚点文件」讲透（每处 ≤25 行关键码），其余一律只给路径进章末脚注清单。禁止逐文件翻 `runtime/src/...`。
2. **API 层讲「怎么用顺手」，实现层讲「为什么这样设计」**。第4章（API 使用）对着 `acl_rt.h` 与 `runtime/example` 讲用法；第5章（实现）才进 `core/src` 讲机制；第6章（驱动）只写用户态可观测的「护照面」，内核侧必须明示「源码不在开源仓」。
3. **运行性分级从严**：`runtime/example` 与 ACL 程序依赖真机 NPU，一律标 `[需真机验证]`（可给 `asc-devkit` 的 cmodel/CPU-SIM 能力作旁证）；严禁把「看起来能跑」标成可运行。

> 执行序与各章方案见 `PLAN-PART2.md`（M1 扩展 B 已完成 → M2 进行中）。
