# 第一编重写方案（PLAN – Part 1）

> 状态：**已交付（三轮迭代完成）**。实际执行按「字数服从质量」原则，三章落于 2.9~4.2k 总字；单章拉长至 8k 是否必要请评审定夺。
> 评审要点：叙事线索是否流畅？由浅入深是否到位？行内引用是否清零？

## 0. 为什么重写（问题诊断）

现有第1–3章就读者的真实病灶：

1. **没有一条「人味」的学习主线**。第1章像 API 目录索引，第2章像硬件手册，第3章像流水线原理图——对第一次接触昇腾的人，知识密度过高且无先后关系。
2. **行内 `📄 资料: …` / `📦 源码: …` 标注密布正文**，平均每 3~5 行断一次，阅读被频繁打断；「溯源正确」被做成了「行内注释」。
3. **代码引用远多于讲解**，且常是「贴一大段再逐点解释」而非「先用一句话说人话，再给关键几行」。
4. 硬件与软件栈**穿插不连贯**：读者还没建立 AI Core 的直觉，就被拉去读 MTE 路径/流水线枚举。

## 1. 重写总原则（六条）

1. **叙事优先**：每章是一条「故事线」，一章只完成一个心智升级；先给生活化模型，再给正式术语（先「打菜/厨房」，后「MTE2/UB」）。
2. **行内引用清零**：正文不再出现 `📄 资料:` / `📦 源码:` 行内标注。改用轻量脚注 `[^n]`，完整出处（路径 + 一句话用途 + 许可证）统一聚合在**章末「本章来源与进一步阅读」**。
3. **代码瘦身**：每段代码先有「这段在做什么」的一句话人话；真码只保留能讲清机制的关键行；大段完整代码移到后续专门章节或附录，不再堆在总览编。
4. **一动一套模型**：第1章给「纵向剖面」，第2章给「AI Core 与搬运-计算模型」，第3章给「任务一生与成本」——三章三块，不贪多。
5. **先动机后机制**：每个概念先回答「为什么需要它/凭什么存在」，再讲「它是什么」。
6. **一章一图**：每章末尾给一张自绘的「小结图/表」，把本章心智模型固化成一张可记忆的画面。

## 2. 三章新结构

### 第1章 一段加法背后的整个软件栈（从你最熟悉的代码出发）

目标心智升级：**「我的每一行 torch 代码，最后都会变成晶圆上一小段执行」——第一次看清全栈纵深，且不背任何 API。**

| 节 | 讲什么 | 怎么引（比喻/例子） | 产出 |
|---|---|---|---|
| 1.1 你会遇到的三台昇腾 | A2/A3/950（PR/DT）一句话差异、术语澄清、Kirin X90 说明外延 | 用「三代机」类比；**不列表格堆规格** | 名词不慌 |
| 1.2 `10` 行 PyTorch 之后发生了什么 | eager 调用栈纵向剖面：框架→适配→ACL→runtime→队列→驱动→AI Core | 一张「电梯剖面图」逐层点亮，逐层一句话 | 能指出自己在哪层 |
| 1.3 第一个 C 程序：aclnn 加法 | 人话版伪码 → 关键 15 行真码；两段式动机 | 先讲「先问尺寸再下单」 | 认识 aclnn 长什么样 |
| 1.4 AI Core 是一间「厨房」 | Scalar 指挥官 / Vector 加工组 / Cube 矩阵车间 / MTE 运输队 / 案板 GM·L1·UB | **比喻先行，术语后置**；点到为止 | 对硬件有个可想象的模型 |
| 1.5 全书的吃法 | 两条路径、编地图、「同一个问题看三处」方法 | — | 会自己导航 |
| 章末 | 本章来源脚注清单 + 小结图 + 进一步阅读 | — | 可溯源 |

> 从旧第1章迁走：ACL 接口地图、错误码体系 → **第4章**；grep 考古手册 → **附录E**；CUDA 对照表 → 保留并嵌进 1.2/1.4 对应小段（不再独立成节）；三视图 Add 代码详解 → 迁到**第2章2.2** 与 **第14章**。

### 第2章 昇腾硬件：搬运-计算模型（由浅入深）

目标心智升级：**「算子复杂度的本质是『怎么把数据搬到位 + 各单元按序吃』，性能天花板在搬运不在算」。**

| 节 | 讲什么 | 怎么引 | 产出 |
|---|---|---|---|
| 2.1 先破一个迷思 | 为什么「搬运」「同步」是瓶颈（一个手算带宽/计算比的小例子，不引厂商数字） | 从 1.4 厨房引伸：备菜比炒菜慢 | 会看瓶颈 |
| 2.2 一个 Add：从 GM 到结果的一趟 | 逐步扩图：GM→..→UB→Vector→..→GM；每步讲「为什么必须这样」；与第1章 aclnn 程序对上 | 拿第1章那盘菜亲手做一遍 | 第一张搬运路线图 |
| 2.3 厨房越开越大：多单元与多层存储 | Vector 拖家带口、Cube 的 L0A/L0B/L0C、L1 为什么出现、MTE1/2/3 与 FixPipe 分工 | 从一道菜到流水席：为何需要中转 | 记住「谁能搬谁、往哪搬」 |
| 2.4 大家各干各的：异步与同步 | 各单元异步干活的必然性；同步=「喊一嗓子」；轻提 PIPE 概念 | 后厨小组各自出菜、要数量对齐 | 理解 `set_flag/barrier` 是干嘛的 |
| 2.5 三个世界再访 | 2201 vs 3510：Membase/Regbase、N-DMA、SHMEM/URMA 预告；「能力菜单」思维 | 新旧厨房对比 | 知道 950 的差异方向 |
| 章末 | 自绘「完整内存层次 + 单元 + 搬运路线」一张图 + 来源脚注 | — | 一张能背的图 |

> 从旧第2章迁走：PIPE 枚举与 `asc_lock/asc_unlock` 完整例、MTE 路径详表 → **第8/11章**；URMA/UDMA put 五步 → **第18章**；本编只保留「名字 + 动机」。

### 第3章 软件执行主链路：一次算子的一生（由浅入深）

目标心智升级：**「图模式/融合优化的本质是『少下几次订单 / 一次多订几道菜』，成本模型 = Host 调度 + 设备执行 + 搬运带宽」。**

| 节 | 讲什么 | 怎么引 | 产出 |
|---|---|---|---|
| 3.1 第一张订单：eager 路径召集 | 从 torch_npu 到 aclnn 两段式到 runtime；为什么订单要「先问尺寸」 | 餐厅点餐类比：先报人数桌型再下单 | 理清 eager 链路 |
| 3.2 后厨怎么接单：流、任务与 SQE | 流=一条取菜带、任务=一张订单、SQE=订单的机器可读版；**不贴 task_to_sqe 代码**，只讲形态 | 用「取菜带」统一解释流/事件 | 还异步的债 |
| 3.3 从队列到芯片：BQS 与 TSD | 订单上车（队列满/不满）、车上调度员（TSD）、做完怎么通知 | 外卖柜：满了会响铃，取走才腾位 | 看懂「最后一段」 |
| 3.4 为什么大模型要整单下：图模式 | eager vs 图；aclGraph 捕获-重放（sink SQE 一句话带过）；AOT SuperKernel 合并订单；给官方收益数字但只作佐证 | 单人点单 vs 月结统采 | 建立图优化直觉 |
| 3.5 三笔账：算子成本心智模型 | Host 调度开销 / 设备执行 / 搬运带宽 —— 本书后文的通用标尺 | 一句话公式 | 后文全都用得上 |
| 3.6 每一层去哪看源码 | 列出「纵深线索」一张表（纯指引，无标注铺陈） | — | 想深挖的人有路 |
| 章末 | 来源脚注 + 小结图 | — | 可溯源 |

> 从旧第3章迁走：BQS 三类事件/TSD 客户端文件细节 → **第5章**；GE/torchair 对比 → **第22章**；错误码 107000/145000 详述 → **第4章**；timestamp 线程/设备推演 → **第15章**。

## 3. 表达规范（本轮强制）

1. **正文零 `📄/📦` 行内标注**：全部改为脚注 `[^n]`，章末聚合：`路径 | 用途一句话 | 状态(许可证)`。附录D源映射表继续维护，作为总账本。
2. **代码四句法**：①一句话人话总述 → ②关键行真码（≤25行）→ ③机制解释 → ④可运行性提示（整段只标一次，不逐行）。
3. **比喻先行术语后置**：先「厨房/订单/取菜带」，再点名 MTE/UB/SQE。
4. **一章一图**：第1章剖面图、第2章搬运路线总图、第3章订单时序图（自绘 SVG/Mermaid，统一风格存 `figures/`）。
5. 每章结尾：`一句话总结` 加粗 + `本章来源与进一步阅读`（脚注聚合）。

## 4. 字数与验收

- 三章目标：**每章 8k–10k 中文字**；以「读得顺」优先，字数服从质量，不灌水。
- 验收清单（评审门 M1'）：
  1. `npm run verify` 全绿（build/站内链接/源码考古/术语/字数）；
  2. 正文 `grep '📄\|📦'` 结果为空（引用全部下移到脚注/章末）；
  3. 「先人话后术语」抽查：随机挑 5 个概念，文中都能找到对应比喻与逐步引入；
  4. 章节末尾均有脚注聚合清单 + 小结图；
  5. 旧内容**零丢失**：迁移清单全部落到目标章节的 source-map（附录D 同步更新）。

## 5. 落地顺序（批准后）

1. 统一改 `STYLEGUIDE.md`：行内引用→脚注规则、代码四句法、比喻先行；附录E 同步。
2. 自绘 3 张图（figures/）。
3. 按第1→2→3章顺序重写；每章重写后立即 `verify` + 措辞检查。
4. 迁移清单同步 appendD/CHANGELOG。
